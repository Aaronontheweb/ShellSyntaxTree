{
  "name": "Runtime PowerShell braced variable fails closed",
  "input": "Get-Content \"${braced-name}\"",
  "expected": {
    "isUnparseable": false,
    "clauses": [{
      "operator": "None",
      "verb": ["Get-Content"],
      "args": [{ "raw": "\"${braced-name}\"", "kind": "DynamicSkip", "isPath": false }],
      "redirects": []
    }]
  },
  "notes": "A braced variable name is a recognized expansion rather than an exact literal filename."
}
