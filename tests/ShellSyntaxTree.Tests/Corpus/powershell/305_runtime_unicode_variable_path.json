{
  "name": "Runtime PowerShell Unicode variable fails closed",
  "input": "Get-Content \"$é\"",
  "expected": {
    "isUnparseable": false,
    "clauses": [{
      "operator": "None",
      "verb": ["Get-Content"],
      "args": [{ "raw": "\"$é\"", "kind": "DynamicSkip", "isPath": false }],
      "redirects": []
    }]
  },
  "notes": "Unicode variable names are recognized expansions rather than literal filenames."
}
