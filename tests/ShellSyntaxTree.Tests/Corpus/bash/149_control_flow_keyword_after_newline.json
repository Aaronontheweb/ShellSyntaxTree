{
  "name": "Control-flow keyword after a newline is unparseable",
  "input": "echo hi\nfor i in 1 2 3",
  "expected": {
    "isUnparseable": true,
    "unparseableReasonContains": "missing its list terminator"
  },
  "notes": "The supported for-in grammar still fails atomically when its list terminator and do block are absent."
}
